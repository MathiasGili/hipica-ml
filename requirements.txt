# ============================================================================
# Hipica-ML — Python dependencies (Python 3.10+)
# ----------------------------------------------------------------------------
# Single requirements file used by every service so that the *exact same*
# feature-engineering code runs in the training notebook and in the FastAPI
# container. This is one of the guards against training-serving skew.
# ============================================================================

# ---------- Core data ----------
pandas>=2.2.0,<3.0
numpy>=1.26.0,<2.2
pyarrow>=15.0.0
openpyxl>=3.1.2          # .xlsx
xlrd==2.0.1              # legacy .xls (Maroñas "tabuladas")

# ---------- Database ----------
SQLAlchemy>=2.0.30,<3.0
psycopg2-binary>=2.9.9
alembic>=1.13.0

# ---------- ML ----------
scikit-learn>=1.5.0,<2.0
xgboost>=2.1.0           # 2.x ships CUDA wheels; uses tree_method='hist', device='cuda'
joblib>=1.4.0
shap>=0.46.0             # explainability (electivo)

# ---------- Experiment tracking ----------
mlflow>=2.16.0,<3.0

# ---------- Scraping ----------
requests>=2.32.0
beautifulsoup4>=4.12.3
lxml>=5.2.0
playwright>=1.45.0       # for any JS-rendered calendar pages on hipica.maronas.com.uy
tenacity>=8.3.0          # retry/backoff for HTTP

# ---------- API ----------
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0,<3.0
pydantic-settings>=2.3.0
httpx>=0.27.0

# ---------- UI ----------
streamlit>=1.36.0
plotly>=5.22.0

# ---------- Utilities ----------
python-dotenv>=1.0.1
loguru>=0.7.2
tqdm>=4.66.0
typer>=0.12.0            # CLI entry-points for scraper / training

# ---------- Dev / Test ----------
pytest>=8.2.0
pytest-cov>=5.0.0
ipykernel>=6.29.0
ruff>=0.5.0
